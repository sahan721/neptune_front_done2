Fix the Admin Leaderboard frontend integration.

DO NOT change any database data or backend logic.

Current situation:
- The backend should return existing leaderboard data again.
- The frontend must correctly display the returned data.

Requirements:

1. All Time must display all data returned by the backend.
2. This Month must display current-month data.
3. Specific Date must send the selected date to the API.
4. Do not show "No records found" when valid API data exists.
5. Inspect the actual API response in the browser Network tab before mapping the response.
6. Fix any mismatch between the backend response structure and frontend code.

Safely handle possible responses:
- []
- { data: [] }
- { leaderboard: [] }
- valid populated responses

Do not assume the response array is always at one specific property without checking the real API response.

For Specific Date:
- Send the selected date as YYYY-MM-DD.
- Do not convert the date incorrectly because of timezone issues.
- Do not use the browser's UTC conversion if it changes the selected calendar day.

Example:
Selected date in UI: 23 Aug 2026
API request must use:
2026-08-23

If the API returns data:
- Display the leaderboard.

If the API returns an empty array:
- Display the existing empty-state message.

If the API fails:
- Display an error message instead of a blank page.

Do not redesign the UI.
Do not remove existing features.
Do not modify database data.
