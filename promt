Implement ONLY the frontend changes required to make the Admin Leaderboard correctly show data for a selected specific date.

Problem:
The Leaderboard already has a Specific Date option and date picker, but selecting a date still shows all-time data. The frontend must send the selected date correctly to the real backend API and display only the returned data.

Requirements:

1. Inspect the existing Leaderboard page and its current API request logic before making changes.

2. Keep the existing period options:
   - All Time
   - This Month
   - Specific Date

3. When All Time is selected:
   - Use the existing all-time leaderboard API behavior.

4. When This Month is selected:
   - Use the existing monthly leaderboard API behavior.

5. When Specific Date is selected:
   - Show the date picker.
   - Require a date before fetching data.
   - Send the selected date to the backend in exactly this format:
     YYYY-MM-DD

   Example:
   /admin/leaderboard?date=2026-08-19

6. Do NOT perform client-side fake filtering if the backend is supposed to filter the data.
   The frontend must request the correct filtered data from the backend.

7. When the user changes the date:
   - Refetch the leaderboard using the newly selected date.
   - Replace the old leaderboard results.
   - Do not leave previous all-time data visible.

8. Add proper states:
   - Loading
   - Error
   - Empty state when there are no collections for the selected date

9. The displayed values must come directly from the backend response:
   - Rank
   - Collector
   - Total Weight
   - Total Collections

10. Do not mix data from different periods.
    A selected date must never display cached all-time or monthly results.

11. Preserve the existing leaderboard design and styling as much as possible.

12. Do not modify unrelated pages or functionality.

CRITICAL:
- Do not change the backend.
- Do not invent fake data.
- Do not hardcode leaderboard values.
- Do not change QR-related functionality.
- Do not modify Flutter/mobile app related functionality.
- Do not modify unrelated admin features.

Before finishing:
- Verify All Time works
- Verify This Month works
- Verify Specific Date sends the date in YYYY-MM-DD format
- Verify changing the date updates the API request
- Verify the results displayed correspond only to the selected date
- Verify an empty date result shows a proper empty state
- Run the frontend build/check and fix only errors caused by these changes

At the end, clearly report:
1. Which frontend file(s) were changed
2. The exact API URL used for Specific Date
3. How the selected date is formatted
4. How the page refreshes when the user changes the date
