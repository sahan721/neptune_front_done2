Fix the existing Leaderboard date filtering in the frontend. Do not redesign or change unrelated functionality.

CURRENT BUG:
When the admin selects a specific date, the leaderboard still displays all-time/historical data. Even when selecting tomorrow or another future date with no data, it still shows existing leaderboard records.

REQUIRED BEHAVIOR:

1. Keep the existing leaderboard UI and existing All Time / This Month options.

2. When the admin selects "Specific Date":
   - Show the date picker.
   - Require/select a date.
   - Send the selected date to the real backend API.

3. Use the exact API request:
   GET /admin/leaderboard?date=YYYY-MM-DD

Use the selected date in the correct YYYY-MM-DD format.

4. When the selected date changes:
   - Fetch new data for that date.
   - Replace the currently displayed leaderboard data.
   - Do NOT keep old data visible as the new result.

5. CRITICAL:
   - Do NOT fall back to All Time data if the selected date has no results.
   - Do NOT automatically call the all-time endpoint when a date-specific request returns an empty array.
   - Do NOT silently remove the date parameter.
   - Do NOT use cached all-time data as a fallback.

6. If the backend returns no data for the selected date:
   - Clear the leaderboard rows.
   - Show a proper empty state:
     "No leaderboard data available for this date."

7. Example:
   If today has data and the admin selects tomorrow:
   - The existing today's/all-time records must disappear.
   - The leaderboard must show the empty state.
   - It must NOT show old records.

8. Preserve existing behavior:
   - All Time → fetch/display all-time data.
   - This Month → fetch/display only current-month data.
   - Specific Date → fetch/display only that exact date's data.

9. Add proper loading and error states.

10. Ensure the date-specific API request uses the real deployed backend URL and existing API configuration.

IMPORTANT:
Do not use mock data.
Do not hardcode results.
Do not fall back to previous leaderboard data.
When the date changes, the displayed data must always match the currently selected filter.

After implementation, test:
- All Time.
- This Month.
- A date containing data.
- Tomorrow/future date containing no data.
- Switching repeatedly between filters.

Verify that selecting tomorrow with no records shows:
"No leaderboard data available for this date."
and NOT all historical leaderboard data.

Run the frontend build and fix all errors.
