URGENT: Fix the Leaderboard Specific Date white-screen error.

Current behavior:
- Leaderboard works normally for All Time.
- When I select a Specific Date, the whole page becomes a blank white screen.
- This must be fixed.

IMPORTANT:
Do not change unrelated UI or redesign the leaderboard.

Requirements:

1. Selecting a specific date must NEVER cause the whole React page to crash or become blank.
2. Check the browser console/runtime errors and identify the exact line causing the crash.
3. Add proper loading, error, and empty-state handling.
4. The date picker must send the selected date to the backend in YYYY-MM-DD format.
5. Use the date-specific leaderboard API contract:
   GET /admin/leaderboard?date=YYYY-MM-DD
   Example:
   GET /admin/leaderboard?date=2026-08-24

6. When the user selects a date:
   - Call the API with the date query parameter.
   - Do NOT continue displaying the previous All Time data.
   - Replace the leaderboard data with only the data returned for that selected date.

7. If no data exists for the selected date:
   - Show a proper message such as:
     "No leaderboard data available for this date."
   - Do NOT show All Time data.
   - Do NOT crash.

8. If the backend returns an error:
   - Show a visible error message.
   - Do NOT render a blank white screen.

9. Safely handle:
   - null response
   - undefined response
   - empty array
   - missing leaderboard fields
   - API/network errors

10. Do not assume that data[0] exists.
    Do not use .map(), .reduce(), or other operations on undefined values.

11. Keep the existing All Time and This Month functionality working.

12. Add an error boundary or equivalent protection so an unexpected component error does not destroy the entire page.

After fixing:
- Test All Time.
- Test This Month.
- Test a date with data.
- Test a date with no data, including tomorrow.
- Confirm tomorrow shows an empty-state message instead of a white screen.

Do not modify backend code. Only fix the Admin frontend integration and UI error handling.
