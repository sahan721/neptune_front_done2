# NEPTUNE ADMIN FRONTEND — ADD RIDER VEHICLE ASSIGNMENT ONLY

You are working inside the existing NEPTUNE Admin Dashboard frontend.

The Admin Dashboard is already working correctly with the backend.

I need ONE new feature:

## WHEN CREATING OR EDITING A RIDER, ADMIN MUST BE ABLE TO ASSIGN AN EXISTING VEHICLE

Example:

Existing Vehicles:

* Tuk 1
* Tuk 2
* Bike 1

Rider form:

```text
Full Name: Sahan
NIC: ...
Mobile: ...
Address: ...

Assigned Vehicle:
[ Tuk 1 ▼ ]
```

The selected vehicle must be saved to the backend as the Rider's assigned vehicle.

---

# 🚨 CRITICAL RULE

DO NOT CHANGE ANYTHING ELSE.

The current Admin Dashboard is already working.

Do not refactor unrelated pages.

Do not redesign the dashboard.

Do not modify working Collector functionality.

Do not modify Vehicle CRUD except where absolutely necessary to support the dropdown.

Do not modify Assignments.

Do not modify Collection Requests.

Do not modify Dashboard statistics.

Do not modify Leaderboard.

Do not modify authentication.

Do not modify Settings.

Do not add unrelated features.

This is a targeted Rider → Vehicle assignment feature.

---

# 🚨 DO NOT TOUCH QR

The QR functionality is unrelated to this feature.

DO NOT modify:

* QR UI
* QR fields
* Collector QR
* QR display
* QR generation
* QR verification
* Rider QR scanning
* QR-related API calls

Leave all QR functionality exactly as it currently works.

---

# 🚨 DO NOT TOUCH FLUTTER

This is the Admin frontend.

Do not modify Flutter code.

Do not create mobile UI.

Do not change mobile API assumptions.

The separate Flutter Collector/Rider applications are outside the scope of this task.

---

# STEP 1 — INSPECT EXISTING ADMIN FRONTEND

Before editing:

1. Inspect Rider types.
2. Inspect Rider form/modal.
3. Inspect Rider create API call.
4. Inspect Rider update API call.
5. Inspect Vehicle types.
6. Inspect Vehicle API service.
7. Inspect existing Vehicle list endpoint.
8. Inspect authentication/API client.
9. Inspect existing validation.
10. Inspect existing UI patterns.

Do not rewrite working components.

---

# STEP 2 — ADD VEHICLE TO RIDER TYPE

The Rider frontend model should support the new backend relationship.

Add:

```text
vehicleId
vehicle
```

where appropriate.

The exact TypeScript type should match the updated backend response.

Example:

```text
vehicleId: string | null
```

and optionally:

```text
vehicle?: {
  id: string
  vehicleCode: string
  vehicleType: string
  status: string
} | null
```

Do not use `any`.

Do not use `@ts-ignore`.

Do not hide type errors.

---

# STEP 3 — VEHICLE DROPDOWN

In the Rider Create/Edit form, add:

```text
Assigned Vehicle
```

as a dropdown/select.

When the form opens:

Call the existing Admin Vehicle endpoint:

```text
GET /admin/vehicles
```

Do NOT create another backend endpoint if the existing endpoint already provides the required data.

---

# STEP 4 — ONLY SHOW ACTIVE VEHICLES FOR NEW ASSIGNMENT

The dropdown should show vehicles that are currently ACTIVE.

Example:

```text
Assigned Vehicle
----------------
Tuk 1
Tuk 2
Bike 1
```

Do not show inactive vehicles as selectable options for a new assignment.

Use the real backend `status`.

Do not hardcode the vehicle list.

Do not use mock vehicle data.

---

# STEP 5 — DISPLAY VEHICLE CODE

The vehicle dropdown should primarily display:

```text
vehicleCode
```

Examples:

* Tuk 1
* Tuk 2
* Bike 1

Vehicle type can optionally be displayed for clarity:

```text
Tuk 1 — Tuk
Tuk 2 — Tuk
Bike 1 — Bike
```

Do not add number plate fields.

The system does NOT require number plates.

---

# STEP 6 — CREATE RIDER

When creating a Rider, send the selected:

```text
vehicleId
```

to the backend.

The Rider create payload must remain the existing correct payload plus:

```text
vehicleId
```

Do not send unrelated fields.

Do NOT send:

* vehicleNumber
* vehicleColour
* area
* lastLogin
* assignmentId
* fake vehicle fields

Only the actual backend-supported `vehicleId` should be added.

---

# STEP 7 — EDIT RIDER

When editing an existing Rider:

If the Rider already has:

```text
vehicleId
```

the dropdown must automatically show the currently assigned vehicle.

Example:

```text
Assigned Vehicle
[ Tuk 1 ▼ ]
```

Admin must be able to change it:

```text
Tuk 1
↓
Tuk 2
```

Then save the Rider.

The new vehicle assignment must persist to the backend/database.

---

# STEP 8 — REMOVE VEHICLE ASSIGNMENT

If the backend supports nullable vehicleId, provide a clean option such as:

```text
No Vehicle Assigned
```

This should set:

```text
vehicleId = null
```

Do not invent a fake vehicle ID.

If the current backend does not support clearing the assignment, do not fake it. Report that backend support is required.

---

# STEP 9 — HANDLE VEHICLE ALREADY ASSIGNED

The backend should prevent two Riders from being assigned to the same vehicle.

If the API returns:

```text
409 Conflict
```

show a clear Admin message such as:

```text
This vehicle is already assigned to another rider.
```

Do not silently overwrite another Rider's assignment.

Do not retry automatically.

---

# STEP 10 — VEHICLE STATUS

Only ACTIVE vehicles should normally appear in the assignment dropdown.

However, if an existing Rider is already assigned to a vehicle that has since become inactive, do not silently remove that assignment.

For an existing Rider, the UI may display the currently assigned inactive vehicle as the current value so the Admin understands the existing state.

When changing the assignment, only ACTIVE vehicles should be selectable.

Do not automatically modify vehicle status.

---

# STEP 11 — RIDER LIST

Update the Rider list to show the assigned vehicle where appropriate.

Example:

```text
Rider        Mobile       Assigned Vehicle
Sahan        07XXXXXXXX   Tuk 1
Kamal        07XXXXXXXX   Bike 1
```

If no vehicle is assigned:

```text
No Vehicle
```

Use actual backend data.

Do not display fake vehicle information.

---

# STEP 12 — RIDER DETAILS

If the Admin Dashboard has a Rider details page/modal, display:

```text
Assigned Vehicle
Tuk 1
```

using the real backend relation.

Do not invent a vehicle assignment.

---

# STEP 13 — DO NOT CHANGE VEHICLE CRUD

The existing Vehicle section is already working.

Keep it working.

Admin should still be able to:

* create vehicle
* edit vehicle
* activate vehicle
* deactivate vehicle
* view vehicle list

Vehicles are created using simple identifiers such as:

```text
Tuk 1
Tuk 2
Bike 1
```

No number plate functionality should be added.

---

# STEP 14 — DO NOT CHANGE OTHER ADMIN FEATURES

Do not modify:

* Collector management
* Assignment management
* Collection Requests
* Collection Details
* Dashboard
* Leaderboard
* Authentication
* Settings
* Notifications
* navigation
* layout
* branding

unless a tiny change is technically required to integrate the Rider vehicle field.

---

# STEP 15 — NO MOCK DATA

The dropdown MUST use:

```text
GET /admin/vehicles
```

from the real backend.

Do not hardcode:

```text
Tuk 1
Tuk 2
Bike 1
```

Do not use mock vehicle arrays.

The database is the source of truth.

---

# STEP 16 — ERROR HANDLING

Handle:

### Vehicle API failure

Show an actual error.

Do not show an empty dropdown and pretend there are no vehicles.

### 401

Handle according to the existing authentication system.

### 403

Show appropriate permission error.

### 404

Show appropriate not-found error.

### 409

Show:

```text
This vehicle is already assigned to another rider.
```

### 400

Display backend validation information appropriately.

---

# STEP 17 — LOADING STATES

When opening the Rider form:

```text
Loading vehicles...
```

while the vehicle list is being fetched.

Do not show an empty dropdown before the request finishes.

If no active vehicles exist:

```text
No active vehicles available
```

The Admin should still be able to create a Rider without a vehicle if the backend allows `vehicleId = null`.

---

# STEP 18 — FORM VALIDATION

Do not make vehicle assignment mandatory unless the backend/business requirement explicitly requires it.

The preferred behavior is:

```text
Assigned Vehicle:
[ No Vehicle Assigned ▼ ]
```

with active vehicles available.

Do not change the existing password, NIC, mobile, or other validation.

Only add validation required for the vehicle field.

---

# STEP 19 — TEST THE EXACT WORKFLOW

Test:

### 1. Create vehicle

Create:

```text
Tuk 1
```

Verify it appears in Vehicle management.

### 2. Create Rider

Open Create Rider.

Verify:

```text
Assigned Vehicle
```

appears.

Verify `Tuk 1` appears in the dropdown.

Select `Tuk 1`.

Create Rider.

### 3. Verify persistence

Refresh the page.

The Rider must still show:

```text
Tuk 1
```

### 4. Edit Rider

Change:

```text
Tuk 1
```

to another active vehicle.

Save.

Refresh.

Verify the new vehicle remains assigned.

### 5. Duplicate assignment

Try assigning the same vehicle to another Rider.

The backend conflict should be displayed correctly.

### 6. Inactive vehicle

Deactivate a vehicle.

Verify it cannot be selected for a new assignment.

### 7. Existing Rider without vehicle

Verify the Rider still displays correctly.

### 8. Vehicle API failure

Verify the UI shows an actual error rather than fake empty data.

---

# STEP 20 — BUILD

After implementation:

* run TypeScript checking
* run the production build
* fix all actual type errors
* verify API payloads
* verify API responses

Do not use:

* `any`
* `@ts-ignore`
* fake data
* fake success messages
* hardcoded vehicle lists

to make the build pass.

---

# 🚨 FINAL CHANGE-SCOPE CHECK

Before finishing, inspect the Git diff.

The changes should be limited to the Rider vehicle assignment feature.

There should be NO unrelated changes to:

* Collector
* QR
* Rider QR scanning
* Collection Requests
* Collections
* Assignments
* Dashboard
* Leaderboard
* Authentication
* Settings
* Notifications
* Vehicle CRUD
* unrelated UI components

If unrelated changes were made, revert them.

---

# FINAL RESULT

The finished Admin Dashboard must behave like this:

```text
ADMIN
  ↓
VEHICLES
  ↓
Create "Tuk 1"
  ↓
RIDERS
  ↓
Create Rider
  ↓
Assigned Vehicle dropdown
  ↓
Tuk 1
Tuk 2
Bike 1
  ↓
Select Tuk 1
  ↓
Create Rider
  ↓
PostgreSQL stores Rider.vehicleId
  ↓
Rider displays "Tuk 1"
```

The existing Neptune Admin Dashboard must continue working exactly as before in every other area.

Only add the Rider → Vehicle assignment feature.

Do not change anything else.
