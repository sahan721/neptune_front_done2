NEPTUNE ADMIN FRONTEND — ADD TOTAL COLLECTIONS DASHBOARD CARD

Add a Total Collections metric to the existing Admin Dashboard.

The value MUST come from the real backend API.

Do NOT hardcode the number.
Do NOT calculate it from currently displayed frontend records.
Do NOT use mock data.

==================================================
DASHBOARD CARD
==================================================

Add a statistics card:

Total Collections

The number must represent the total number of actual records in the backend `collections` table.

Example:

┌─────────────────────────┐
│ Total Collections       │
│ 25                      │
└─────────────────────────┘

Use the existing Neptune Dashboard card design and styling.

Do NOT redesign the Dashboard.

==================================================
API
==================================================

Use the existing Admin Dashboard statistics/summary API if one already exists.

The backend developer will provide the final API contract.

The response will contain a field equivalent to:

totalCollections

Use the actual backend field name.

Do NOT guess a different field.

Do NOT create fake data.

==================================================
REAL DATA
==================================================

The displayed number must come from PostgreSQL through:

PostgreSQL
↓
Backend
↓
Admin API
↓
Dashboard

Do NOT connect the frontend directly to Supabase.

Do NOT expose DATABASE_URL or database credentials.

Use:

VITE_API_BASE_URL

for the backend API.

==================================================
LOADING
==================================================

While dashboard statistics are loading:

show the existing dashboard loading/skeleton state.

Do not display a fake number such as 0 unless the existing UI specifically uses 0 as its loading placeholder.

==================================================
ERROR
==================================================

If the statistics API fails:

handle the error using the existing Dashboard error handling.

Do not silently display an incorrect hardcoded value.

==================================================
REFRESH
==================================================

When the Dashboard loads/refetches its statistics, Total Collections must use the latest backend value.

Do not permanently cache the number in frontend code.

==================================================
DO NOT TOUCH
==================================================

Do NOT modify:

- QR
- QR verification
- QR tokens
- Collector QR
- Rider QR scanning
- Flutter/mobile functionality
- Collector management
- Rider management
- Vehicle management
- Rider → Vehicle assignment
- Reports
- Leaderboard
- Collection Requests
- existing dashboard cards

Only add the Total Collections metric and the minimum API integration required.

==================================================
TESTING
==================================================

Verify:

1. Dashboard loads.
2. Total Collections card appears.
3. Number matches the actual `collections` table.
4. Refreshing Dashboard gets the current value.
5. API errors are handled correctly.
6. Existing Dashboard cards still work.
7. TypeScript/build succeeds.

Do not use mock data or hardcoded numbers.

Inspect Git diff and make sure no unrelated functionality was changed.
